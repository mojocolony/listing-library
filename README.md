# Listing Library v0.1.5 update

Replace these three files in the repository root:

- `index.html`
- `app.js`
- `styles.css`

No property images or sample files need to be uploaded again.

Changes: actual Lucide `houses` icon loaded from Lucide Static 1.45.0; All Properties/Favourites views; tag filtering; clickable tags on property pages; Notes renamed Details; missing-media tabs remain hidden; pill centering normalized.
